// The setInterval method is similar to
// setTimeout but it executes a function at
// a certain interval until it is cleared.
/* setInterval(() => {
    console.log("hello my friend")
}, 3000); */
/* setInterval(function(){
    console.log("shevin är bäst")
},1000); */

//make it stop plssssss

/* let count = 0;
const intervalId = setInterval(() => {
    console.log("Here I am");s
    count = count + 1;
    console.log(count);
    if (count === 10) {
        console.log("interval Finished!");
        clearInterval(intervalId);
    }
}, 1000);
 */
/* let dude = 0;
const jagTyckerOmDig = setInterval(() => {
   console.log("du är cool Shvein")
   dude = dude + 1;
   console.log(dude);
   if (dude === 20){
       console.log("Älskar dig för evigt");
       clearInterval(jagTyckerOmDig);
   }
}, 2000); */
/* const list = document.querySelector(".container");
counter = 0;
const minMamma = setInterval(() => {
    counter = counter + 1;
    const listItem = document.createElement("li");
    listItem.innerHTML = counter;
    list.append(listItem);
    if (counter === 10) {
        clearInterval(minMamma);
    }
}, 1000);
 */
